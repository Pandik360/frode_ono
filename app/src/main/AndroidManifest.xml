<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eto_ono">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_orange_samsung_icon_134366"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_orange_samsung_icon_134366"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Graf_activity"></activity>
        <activity android:name=".ThirdActivity" />
        <activity android:name=".Second_activity" />
        <activity
            android:name=".First_activity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>